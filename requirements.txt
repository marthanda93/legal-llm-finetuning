# Runtime deps for the legal-llm-finetuning workbench.
# Tested with Python 3.12 on Apple Silicon (M1/M2/M3) under MLX.

# --- MLX core (Apple Silicon LoRA fine-tune + serving) ---
mlx>=0.21.0
mlx-lm>=0.21.0

# --- LLM I/O (eval harness + teacher_client) ---
httpx>=0.27.0

# --- Training-data builders ---
pyyaml>=6.0.1

# --- Optional: action-summary cosine in `training.eval` ---
# Drop these two if you always pass --no-cosine.
sentence-transformers>=3.0.0
numpy>=1.26.0

# --- Teacher LLM SDKs for Stage C (install whichever you use) ---
openai>=1.40.0           # OPENAI_API_KEY / OPENROUTER_API_KEY
google-generativeai>=0.7.0  # GOOGLE_API_KEY
anthropic>=0.34.0        # ANTHROPIC_API_KEY

# --- Dev / quality of life ---
ruff>=0.6.0
